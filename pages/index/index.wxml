<view class="location">
    <image class="location-icon" src="{{locationSrc}}" bindtap="locationTap"></image>
    <image class="address-icon" src="{{addressSrc}}"></image>
    <view class="location-address">{{address}}</view>
</view>

<view bindtap="getLocation" class="btn-search">搜一搜</view>